#!/bin/sh
set -e

setup() {
  echo $1:
  cd $1 && bin/setup && cd ..
}

test() {
  EBOSHI_API_SHOOTOUT_CURRENT_IMPL=$1 ruby test/test.rb
}

teardown() {
  cd $1 && bin/teardown && cd ..
  echo ""
}

setup $1 && test $1 && teardown $1
